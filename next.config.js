/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['giphy.com', 'media0.giphy.com', 'media2.giphy.com', 'media4.giphy.com'], 
      },
}

module.exports = nextConfig
